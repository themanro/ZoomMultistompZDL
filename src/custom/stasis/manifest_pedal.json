{
  "effect_name": "Stasis",
  "comment": "Footswitch-triggered freeze. Stomp the slot ON and whatever you just played is held indefinitely while the dry keeps passing, so you play over your own sustained chord. Only possible because EdgeWatch proved on hardware that the effect keeps running while bypassed, sees params[0] change from the footswitch, and still receives input audio while bypassed -- so the ring is already full at the moment you stomp and the capture is instantaneous. Sustain is granular rather than looped, because a loop of any length betrays itself as a period.",
  "gid": 8,
  "gid_comment": "0x08 = Delay category, matching the rest of the release pack.",
  "fxid": 440,
  "fxid_comment": "0x1B8. Free: unused by stock effects, not a retired custom alias, and clear of the fxid collisions audited on 2026-08-18.",
  "fxid_version": "1.00",
  "flags_byte": 1,
  "audio_func_name": "Fx_DLY_Stasis",
  "audio_nop": false,
  "params": [
    {
      "name": "Length",
      "max": 100,
      "default": 35,
      "comment": "How much of the past is captured AND the loop length, ~60 ms .. ~1.4 s. The capture looks BACKWARDS from the moment you stomp -- Stasis records continuously while the slot is off and stops when it is on, so it holds what you just played rather than making you play into it."
    },
    {
      "name": "Blur",
      "max": 100,
      "default": 50,
      "comment": "Crossfade length at the loop seam, 5.8..93 ms. Short is a tighter, more defined hold; long smears the join away. Clamped so it always fits twice inside the loop. Was a grain size in v1, when playback used random grain offsets and chopped sustained notes."
    },
    {
      "name": "Decay",
      "max": 100,
      "default": 100,
      "comment": "How the hold fades. Fully up is genuinely infinite."
    },
    {
      "name": "Tone",
      "max": 100,
      "default": 55,
      "comment": "Low-pass on the held layer only; the dry stays open."
    },
    {
      "name": "Mix",
      "max": 100,
      "default": 50,
      "comment": "Level of the held layer. NOT a dry/wet crossfade -- the dry always passes at unity so you can play over the hold at full strength. As a crossfade this cost 6 dB just for having Stasis in the patch, because the wet side is silent until you stomp."
    }
  ]
}
